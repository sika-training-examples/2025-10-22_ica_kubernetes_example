apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: counter-backend-ica
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
  labels:
    project: counter
spec:
  ingressClassName: openshift-default
  tls:
    - hosts:
        - counter-backend-ondrejsika.ica.sikademo.com
      secretName: counter-backend-ondrejsika.ica.sikademo.com-tls
  rules:
    - host: counter-backend-ondrejsika.ica.sikademo.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: counter-backend
                port:
                  number: 80
